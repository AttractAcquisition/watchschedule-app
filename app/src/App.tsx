// Scaffold placeholder. Real routing (BrowserRouter + AuthGate + route map)
// is built starting in Phase 2 per frontend.md §2/§3 and docs/master.md §5.
export default function App() {
  return (
    <main>
      <h1>WatchSchedule</h1>
      <p>Repository skeleton. Build proceeds phase-by-phase per docs/master.md.</p>
    </main>
  )
}
